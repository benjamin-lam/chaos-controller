import { loadEnvForStage } from '@test-platform/core';

/**
 * 🌱 Lädt und validiert Umgebungsvariablen für das E-Commerce-Template inklusive Stage-Unterstützung.
 */
export const env = loadEnvForStage();
