import { globalSetup as coreGlobalSetup } from '@test-platform/core';

/**
 * 🌐 Nutzt das gemeinsame Global-Setup; Login-Selectoren kannst du über .env konfigurieren.
 */
export default coreGlobalSetup;
