import { loadEnvForStage } from '@test-platform/core';

/**
 * 🌱 Lädt und validiert Umgebungsvariablen für das SaaS-Template inklusive Stage-Unterstützung.
 */
export const env = loadEnvForStage();
